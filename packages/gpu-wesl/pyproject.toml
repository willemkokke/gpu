[project]
name = "gpu-wesl"
version = "0.0.0"
description = "WESL extension for WGSL manipulation"
readme = "README.md"
license = "MIT"
authors = [{ name = "Willem Kokke", email = "mail@willem.net" }]
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/gpu"]
